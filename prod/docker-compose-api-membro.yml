version: '3.4'

services:
  aschurchmanager.web:
    image: ${DOCKER_REGISTRY-}aschurchapimembro
    build:
      context: .
      dockerfile: ASChurchManager.API.Membro/Dockerfile
    environment:
     - ASPNETCORE_ENVIRONMENT=Production